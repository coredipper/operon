\* TLC model configuration for ConvergenceDetection.tla
\* Small-model parameters for tractable model checking.

SPECIFICATION FairSpec

\* --- Constants ---

CONSTANT Orgs = {o1, o2}
CONSTANT MAX_RATE = 0.5
CONSTANT TOTAL_STAGES = 5

\* --- State invariants (checked at every reachable state) ---

INVARIANT TypeOK

\* --- Temporal / liveness properties (checked over behaviors) ---
\* HaltIsTerminal uses [][...]_vars; BoundedNonConvergence and
\* ConvergentOrganismCompletes use leads-to (~>). All are temporal formulas.

PROPERTY HaltIsTerminal
PROPERTY BoundedNonConvergence
PROPERTY ConvergentOrganismCompletes
